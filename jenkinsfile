pipeline {
    agent any  // Agent avec Python 3.10.12+ et Docker installé

    environment {
        SONAR_TOKEN = credentials('scanner')  // SonarQube token
        registry = '192.168.33.10:8083'  // Nexus registry
        registryCredentials = 'nexus'  // Credential ID pour Nexus
        imageName = 'pixelette-app'  // Nom de l'image
        imageTag = '1.0'  // Tag par défaut, ajusté avec BUILD_NUMBER
    }

    stages {
        stage('Checkout Code') {
            steps {
                script {
                    echo "🧹 Nettoyage du workspace..."
                    cleanWs()
                    echo "📥 Clonage du dépôt (branche cicd)..."
                    withCredentials([usernamePassword(
                        credentialsId: 'github-token',
                        usernameVariable: 'GIT_USERNAME',
                        passwordVariable: 'GIT_TOKEN'
                    )]) {
                        sh 'git clone -b cicd https://${GIT_USERNAME}:${GIT_TOKEN}@github.com/Helmi-Gargouri/Pixelette.git .'
                    }
                }
            }
        }

stage('Install Python Dependencies') {
    steps {
        script {
            echo "📦 Installation des dépendances Python..."
            sh '''
                # Vérification pip3
                command -v pip3 >/dev/null 2>&1 || { 
                    echo "❌ pip3 non trouvé."; 
                    exit 1; 
                }
                
                # Vérifier si venv est disponible
                if ! python3 -m venv --help > /dev/null 2>&1; then
                    echo "⚠️ python3-venv n'est pas installé. Contactez l'administrateur."
                    exit 1
                fi
                
                # Créer et activer l'environnement virtuel
                python3 -m venv venv
                . venv/bin/activate
                pip install --upgrade pip
                pip install -r requirements.txt
                pip install coverage
            '''
        }
    }
}

stage('Run Tests') {
            steps {
                script {
                    echo "🧪 Exécution des tests Django (64 tests attendus)..."
                    sh '''
                        set -e  # Arrêter en cas d'erreur
                        
                        # Activez l'environnement virtuel
                        . venv/bin/activate
                        
                        # Vérifier que Django est installé
                        echo "🔍 Vérification de l'installation Django..."
                        python -c "import django; print(f'Django version: {django.get_version()}')" || {
                            echo "❌ Django n'est pas installé correctement"
                            exit 1
                        }
                        
                        # Créer le fichier .env si nécessaire
                        if [ ! -f .env ]; then
                            echo "📝 Création du fichier .env..."
                            cat > .env << 'EOF'
DJANGO_SETTINGS_MODULE=Pixelette.settings
DATABASE_URL=sqlite://:memory:
SECRET_KEY=test-secret-key-for-ci
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
EOF
                        fi
                        
                        # Exporter les variables d'environnement
                        export DJANGO_SETTINGS_MODULE=Pixelette.settings
                        export SECRET_KEY=test-secret-key-for-ci
                        export DEBUG=False
                        
                        # Vérifier manage.py
                        if [ ! -f manage.py ]; then
                            echo "❌ manage.py introuvable!"
                            ls -la
                            exit 1
                        fi
                        
                        echo "🔧 Vérification des migrations..."
                        python manage.py makemigrations --dry-run --verbosity 1 --check || {
                            echo "⚠️ Migrations non appliquées détectées"
                        }
                        
                        echo "🗃️ Migration de la base de données..."
                        python manage.py migrate --run-syncdb --verbosity 1
                        
                        # Créer les répertoires pour les rapports
                        mkdir -p build/reports/coverage build/reports/tests
                        
                        echo "🚀 Exécution des tests avec coverage..."
                        set +e  # Ne pas arrêter si les tests échouent
                        coverage run --source=Pixelette manage.py test Pixelette.tests --verbosity 2
                        TEST_EXIT_CODE=$?
                        set -e
                        
                        echo "📊 Génération du rapport de couverture..."
                        coverage xml --include="Pixelette/*" -o build/reports/coverage/coverage.xml || {
                            echo "⚠️ Erreur lors de la génération du rapport XML"
                        }
                        
                        coverage report --include="Pixelette/*" || echo "⚠️ Rapport texte non disponible"
                        
                        echo "📊 Vérification des rapports générés:"
                        ls -lah build/reports/coverage/ || true
                        
                        if [ -f build/reports/coverage/coverage.xml ]; then
                            LINES=$(wc -l < build/reports/coverage/coverage.xml)
                            SIZE=$(du -h build/reports/coverage/coverage.xml | cut -f1)
                            echo "✅ Coverage XML généré: ${LINES} lignes, ${SIZE}"
                            head -n 5 build/reports/coverage/coverage.xml
                        else
                            echo "❌ Coverage XML manquant - génération d'un fallback"
                            cat > build/reports/coverage/coverage.xml << 'EOF'
<?xml version="1.0"?><coverage version="1.0"><metrics files="1" loc="100"/></coverage>
EOF
                        fi
                        
                        # Retourner le code de sortie des tests
                        if [ $TEST_EXIT_CODE -ne 0 ]; then
                            echo "⚠️ Les tests ont échoué avec le code: $TEST_EXIT_CODE"
                            exit $TEST_EXIT_CODE
                        fi
                        
                        echo "✅ Tous les tests sont passés!"
                    '''
                }
            }
            post {
                always {
                    archiveArtifacts artifacts: 'build/reports/coverage/**/*', allowEmptyArchive: true
                }
                success {
                    echo "✅ Tous les tests sont passés avec succès !"
                }
                failure {
                    echo "❌ Échec des tests. Vérifiez les logs ci-dessus pour plus de détails."
                }
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    echo "🔍 Analyse SonarQube..."
                    def scannerHome = tool 'SonarQube Scanner'
                    withSonarQubeEnv('SonarQube Server') {
                        timeout(time: 10, unit: 'MINUTES') {
                            sh """
                                # Activez l'environnement virtuel
                                . venv/bin/activate
                                ${scannerHome}/bin/sonar-scanner \
                                    -Dsonar.projectKey=pixelette \
                                    -Dsonar.projectName=Pixelette \
                                    -Dsonar.host.url=http://192.168.33.10:9000 \
                                    -Dsonar.login=${SONAR_TOKEN} \
                                    -Dsonar.sources=Pixelette \
                                    -Dsonar.tests=Pixelette/tests \
                                    -Dsonar.python.coverage.reportPaths=build/reports/coverage/coverage.xml \
                                    -Dsonar.python.file.suffixes=py \
                                    -Dsonar.exclusions=**/migrations/**,**/static/**,**/media/**,**/admin/** \
                                    -Dsonar.sourceEncoding=UTF-8 \
                                    -Dsonar.verbose=true
                            """
                        }
                    }
                }
            }
            post {
                success {
                    echo "✅ Analyse SonarQube réussie."
                    timeout(time: 10, unit: 'MINUTES') {
                        waitForQualityGate abortPipeline: true
                    }
                }
                failure {
                    echo "❌ Échec de l'analyse SonarQube."
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    echo "🐳 Construction de l'image Docker..."
                    sh '''
                        docker-compose build
                        echo "🔍 Vérification image: ${imageName}:latest"
                        docker images ${imageName} | grep latest || {
                            echo "❌ Image ${imageName}:latest manquante!"
                            exit 1
                        }
                    '''
                }
            }
        }

        stage('Deploy to Nexus') {
            steps {
                script {
                    echo "📦 Déploiement vers Nexus (${registry})..."
                    def imageTagFinal = env.BUILD_NUMBER ?: env.imageTag
                    def fullImage = "${registry}/${imageName}:${imageTagFinal}"
                    sh '''
                        set -e
                        echo "🔍 Vérification image: ${imageName}:latest"
                        docker images ${imageName} | grep latest || {
                            echo "❌ Image ${imageName}:latest manquante!"
                            exit 1
                        }
                    '''
                    withCredentials([usernamePassword(
                        credentialsId: registryCredentials,
                        usernameVariable: 'NEXUS_USER',
                        passwordVariable: 'NEXUS_PASS'
                    )]) {
                        sh """
                            echo "🔐 Login ${registry}..."
                            echo "\${NEXUS_PASS}" | docker login ${registry} -u "\${NEXUS_USER}" --password-stdin
                            echo "🏷️ Tag: ${fullImage}"
                            docker tag ${imageName}:latest "${fullImage}"
                            echo "🚀 Push ${fullImage}..."
                            docker push "${fullImage}"
                            docker logout ${registry}
                            echo "🔒 Logout OK"
                        """
                    }
                    env.DEPLOYED_IMAGE = fullImage
                    echo "✅ Image déployée: ${fullImage}"
                }
            }
        }
    }

    post {
        always {
            echo "🧹 Nettoyage..."
            archiveArtifacts artifacts: 'build/reports/coverage/*', allowEmptyArchive: true
            publishHTML([
                allowMissing: true,
                alwaysLinkToLastBuild: true,
                keepAll: true,
                reportDir: 'build/reports/coverage',
                reportFiles: 'index.html',
                reportName: 'Coverage Report'
            ])
            cleanWs()
        }
        success {
            echo "🎉 Pipeline exécuté avec succès !"
        }
        failure {
            echo "❌ Le pipeline a échoué. Vérifiez les logs."
        }
    }
}