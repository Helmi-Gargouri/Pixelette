import React from 'react'

export default function Home() {
  return (
    <div style={{ padding: 20 }}>
      <h1>ArtVista React (Starter)</h1>
      <p>This wraps the original static template pages into a React router-based app.</p>
      <p>To display the original pages, ensure the original `assets/` and HTML pages are copied into <code>/public</code>.</p>
      <p>
        Example: <a href="/pages/index.html">Open original index.html</a>
      </p>
    </div>
  )
}
